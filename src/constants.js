'use strict';

const DEFAULT_COMMAND = `--help`;
const USER_ARGV_INDEX = 2;
const ExitCode = 0;

const MAX_ID_LENGTH = 6;

module.exports =
{
    DEFAULT_COMMAND,
    USER_ARGV_INDEX,
    MAX_ID_LENGTH,
    ExitCode
};
